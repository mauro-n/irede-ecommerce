export type anyObject = {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    [key: string]: any
}

export type category = {
    id: string,
    title: string,
    img: string,
    created_at: string,
    updated_at: string
}